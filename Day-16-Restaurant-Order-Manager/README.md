# 🍽️ Day 16 - Restaurant Order Manager (Review Challenge)

## 📖 Project Overview

This project is a review challenge that combines everything learned from Days 11–15. It simulates a simple restaurant ordering system where orders can be added, removed, prioritized, and displayed dynamically using JavaScript.

## ✨ Features

- Stores restaurant orders in a JavaScript array
- Adds a new order using `push()`
- Removes the last order using `pop()`
- Adds a VIP order to the beginning using `unshift()`
- Removes the first order using `shift()`
- Displays all current orders dynamically with a `for` loop
- Uses a function to organize and display the order list

## 🧠 What I Learned

- Creating and calling functions
- Using arrays to store multiple values
- Adding items with `push()`
- Removing items with `pop()`
- Adding priority items with `unshift()`
- Removing the first item with `shift()`
- Looping through arrays with `for`
- Using `.length` to control loops
- Generating HTML dynamically with `output +=`
- Updating the webpage using `innerHTML`

## 🛠️ Technologies Used

- HTML5
- CSS3
- JavaScript (ES6)

## 📂 Project Structure

```
Day-16-Restaurant-Order-Manager/
│── index.html
│── style.css
│── script.js
│── README.md
```

## 🎯 Skills Practiced

- Functions
- Function Calls
- Arrays
- Array Methods (`push()`, `pop()`, `shift()`, `unshift()`)
- For Loops
- Template Literals
- DOM Manipulation
- Dynamic HTML Generation

## 📸 Sample Output

```
🍽️ Restaurant Order Manager

1. Jollof Rice
2. Chicken
3. Soft Drink
```

Although new orders were added and removed during the program, the final displayed list contains the remaining active orders after all operations were completed.

---

## 🚀 Part of My 30 Days JavaScript Bootcamp

**Day 16 Complete ✅**

### Key Concepts Reviewed

- Functions
- Arrays
- Loops
- DOM Manipulation
- Array Methods

This project serves as a review challenge combining everything learned from Days 11–15 into one application.